path:
  generate-doc-html-dir: "/home/ubuntu/WAS/src/main/resources/static"
  SSG-root: "/home/ubuntu/static-site-generator/"
  clone-dir: "dd"

spring:
  application:
    name: apidoc43
  #TODO ?? commoon-domain ? Entity? ?????? ????. ?? ???? ??? ??
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  datasource:
    url: jdbc:mysql://localhost:3306/sdgb?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ''
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    mongodb:
      uri: mongodb://localhost:27017/test

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: <your-client-id>
            client-secret: <your-client-secret>


openai:
  api-key: "f"